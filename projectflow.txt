1. create git repo and clone it to local
2. create mycode.py and add code to it . will save data to new folder
3. do a git add-commit-push before initializing dvc.
# pip install dvc
4. now we do dvc init  (creates .dvcignore , .dvc)
5. now do mkdir S3 ( creates a new S3 directory)
6. Now we do 'dvc remote add -d myremote S3'
7. next 'dvc add data/' 
we will have to  remove git from folder for dvc to  handle
by git rm -r --cached 'data'  #data is file name.
8. dvc add data/    git add .gitignore data.dvc
9 . Now dvc commit and then dvc push
9 . git add -commit-push to mark this stage as first version of data.
10. now make change to mycode.py to append a new row in data , check changes via 'dvc status'
11.  again  "dvc commit" and "dvc push" and generate a new id corresponding to data 
and new data pushed to S3
12. then git add -commit  -push ( we are saving v2 of our data )
13.check dvc git status 
13 repreat for v3 . 

